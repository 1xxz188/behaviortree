## 项目约定
- **protobuf**：使用 `google.golang.org/protobuf/proto` 的 `Marshal`/`Unmarshal`；
- **测试用例**：需加中文注释说明功能
- **代码注释**: 新增的函数、复杂的函数步骤、结构体和结构体成员都需要有必要的中文注释
- 实现必须考虑性能问题，不能存在大量遍历、多次重复IO、定时轮询等操作，优先O(1)方案

## MCP 与多代理协作
- 每个任务开始时，先判断已配置的 MCP 和子代理是否适用。
- 涉及 Go 语义分析、引用、诊断时，优先使用 gopls MCP。
- 涉及跨模块调用链和代码结构探索时，优先使用 codegraph MCP。
- 当任务可以拆成两个或更多互不依赖的探索、审查、测试或日志分析子任务时，必须主动委派给子代理并行执行。
- 不允许多个子代理同时修改同一文件；并行写入存在冲突风险时，由主代理统一实施修改。
- 主代理必须等待所需子代理返回，核验结果后再给出最终结论。
- 简单问答、单步修改或强耦合任务可以不委派，禁止为了形式而调用无关 MCP 或子代理。
- 最终答复中简要列出实际调用的 MCP 服务和子代理；未调用时说明原因。
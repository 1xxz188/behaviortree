import type { NodeType } from "./enums.ts";

// 内置节点的详细使用指南，与节点库中的简短提示分开维护。
export interface NodeHelp {
  description: string; // 节点职责及推进方式。
  results: string[]; // 成功、失败和运行中的返回规则。
  configuration: string[]; // 子节点结构及需要配置的属性。
  scenario: string; // 适合采用该节点的业务场景。
  example: string; // 具体配置和可观察的执行过程。
}

// 按类型直接读取；穷举约束保证新增内置类型时同步补齐说明。
export const nodeHelp: Record<NodeType, NodeHelp> = {
  sequence: {
    description: "按配置的子节点顺序逐个执行，适合必须依次完成的流程。运行中的子节点会保留进度，恢复时从该子节点继续。",
    results: ["全部子节点成功才返回成功；任一子节点失败立即返回失败，不再执行后续步骤。", "子节点返回 Running（运行中）时，整个顺序节点也保持 Running。整体结束后再次进入会重新开始。"],
    configuration: ["连接需要顺序执行的子节点，在属性面板调整子节点顺序；画布坐标不决定执行顺序。"],
    scenario: "任务步骤存在前后依赖，例如检查目标、移动到目标、执行交互。",
    example: "顺序 → 检查目标有效 → 移动到目标 → 拾取物品。移动尚未完成时不会拾取；检查失败则整个流程失败。",
  },
  selector: {
    description: "按配置顺序尝试多个备选方案，前一个失败才尝试下一个。保留正在运行的分支，恢复时继续该分支。",
    results: ["任一子节点成功立即返回成功；全部子节点失败才返回失败。", "当前分支 Running 时保持 Running，不会每次恢复都重新尝试排在前面的方案。"],
    configuration: ["将优先尝试的方案排在前面，兜底方案放在最后；需要中途抢占时使用优先级节点。"],
    scenario: "有多个可替代的处理方式，希望按顺序找到能够完成任务的方案。",
    example: "选择 → 近战攻击 → 远程攻击 → 待机。近战条件不满足而失败时尝试远程；远程成功后不再待机。",
  },
  priority: {
    description: "依据条件选择优先级最高的可用分支。相关依赖发生变化时重新评估，高优先级分支可抢占当前动作，进入新分支前先取消旧分支。",
    results: ["所选分支的执行结果决定节点结果；分支正在运行时保持 Running。", "没有可用分支时返回失败；最后一个兜底分支可用于处理其他情况。"],
    configuration: ["越靠前的子节点优先级越高。前面的候选分支必须为顺序节点（Sequence），且第一个子节点必须为条件节点（Condition）。", "最后一个候选可作为无条件兜底；条件依赖通过黑板变化或声明的业务事件通知，不能依赖循环轮询。"],
    scenario: "角色需要随状态变化立即切换行为，例如逃跑优先于战斗，战斗优先于巡逻。",
    example: "优先级 → 顺序（低血量？→ 逃跑）→ 顺序（发现敌人？→ 攻击）→ 巡逻。巡逻时血量变化满足逃跑条件，会先取消巡逻再开始逃跑。",
  },
  parallel: {
    description: "在同一线程中依次推进多个分支，让多个异步动作的生命周期重叠；不会为每个子节点创建线程或 goroutine。",
    results: ["全部分支成功才返回成功。任一分支失败立即失败，并取消其他仍在运行的分支。", "尚有分支 Running 且没有失败时保持 Running；恢复时只推进需要处理通知的分支。"],
    configuration: ["连接需要同时推进的子节点；本节点采用全部成功规则，不提供成功数量阈值。"],
    scenario: "多个独立动作需要一起启动，并等待全部完成后进入后续流程。",
    example: "并行 → 移动到集合点、播放语音。语音先结束时仍等待移动；移动失败时会取消尚未结束的语音。",
  },
  condition: {
    description: "调用绑定的 Go 条件函数，同步检查业务状态并返回布尔结果。可作为普通流程检查，也可作为优先级分支的守卫。",
    results: ["Go 函数返回 true 对应成功，false 对应失败。条件同步完成，不返回 Running。"],
    configuration: ["绑定条件类型的 Go 业务定义，并配置其参数；参数可使用常量或黑板字段。", "条件是叶节点，不能连接子节点；需要因外部状态变化重评时，配置相应依赖并由宿主通知变化。"],
    scenario: "判断血量阈值、目标可见性、任务是否完成等业务条件。",
    example: "条件绑定 IsHealthLow，阈值参数为 30。血量为 20 时成功，为 80 时失败；放在逃跑分支开头可控制是否进入逃跑流程。",
  },
  action: {
    description: "调用绑定的 Go 动作函数执行实际业务。动作支持开始（Start）、恢复（Resume）和取消（Abort），可同步完成，也可等待外部通知。",
    results: ["业务完成后返回成功或失败；尚未完成返回 Running。", "异步通知触发后恢复动作；被抢占、超时或父流程取消时，通过 Abort 处理清理。"],
    configuration: ["绑定动作类型的 Go 业务定义，并配置其参数；动作是叶节点，不能连接子节点。", "异步动作需由业务实现通知与取消逻辑；拖入定义只创建实例，不自动实现 Go 业务代码。"],
    scenario: "移动、播放动画、发送请求、拾取物品等会改变业务状态的操作。",
    example: "动作绑定 MoveTo，目标参数引用黑板中的目标实体。开始移动后返回 Running，到达通知后恢复并成功；被逃跑分支抢占时取消旧移动。",
  },
  wait: {
    description: "等待指定时长后继续流程，使用宿主的一次性定时通知，不通过循环轮询检查时间。",
    results: ["等待期间返回 Running，到期后返回成功。"],
    configuration: ["配置等待时长，单位为毫秒（ms）；等待是叶节点，不能连接子节点。"],
    scenario: "流程间隔、演出停顿、重试前的固定延时。",
    example: "顺序 → 播放对话 → 等待 500 ms → 显示选项。对话成功后停顿半秒，再显示选项。",
  },
  timeout: {
    description: "为一个子节点设置执行时限。启动子节点后等待其结束或超时通知，避免流程无限期等待。",
    results: ["子节点在时限内结束，沿用其成功或失败结果；未结束时保持 Running。", "超时到期后取消子节点并返回失败。"],
    configuration: ["必须连接一个子节点，并配置超时时长，单位为毫秒（ms）。", "子节点若包含异步动作，应实现取消处理，以停止对应业务操作。"],
    scenario: "给外部请求、寻路、等待响应等操作设置最长执行时间。",
    example: "超时 2000 ms → 请求路径规划。800 ms 内拿到结果则正常结束；超过 2 秒仍未结束则取消请求并失败。",
  },
  repeat: {
    description: "反复执行同一个子节点，只有上一轮成功才开始下一轮，用于有限次数的重复任务。",
    results: ["累计完成指定次数后成功；任意一轮失败立即失败。", "子节点 Running 时保留当前轮，不增加已完成次数。"],
    configuration: ["必须连接一个子节点，并配置有限的执行次数；次数包含首次执行，3 表示总共执行 3 次。", "节点本身不增加轮次之间的等待；需要间隔时可在子流程中加入等待节点。"],
    scenario: "固定次数的巡查、重复播放或分批处理流程。",
    example: "重复 3 次 → 顺序（巡查一圈 → 等待 1000 ms）。三轮全部成功后结束；第二轮巡查失败则不执行第三轮。",
  },
  retry: {
    description: "子节点失败后重新尝试，直到成功或达到总尝试次数，用于允许失败后再次执行的操作。",
    results: ["任一次成功立即成功；所有尝试都失败后返回失败。", "子节点 Running 时等待当前尝试结束，不增加次数，也不启动新的尝试。"],
    configuration: ["必须连接一个子节点，并配置有限的尝试次数；次数包含首次尝试，3 表示最多尝试 3 次。", "不会自动增加重试间隔；需要延迟时应通过子流程中的等待节点明确安排。"],
    scenario: "短暂失败后可能恢复的交互或请求；应确保业务允许重复执行。",
    example: "重试 3 次 → 尝试开门。第一次失败、第二次成功时立刻结束；连续失败三次后交给外层选择节点执行备用方案。",
  },
  inverter: {
    description: "将子节点的成功和失败结果互换，复用已有判断实现相反条件。",
    results: ["子节点成功变为失败，失败变为成功；Running 保持 Running。"],
    configuration: ["必须连接一个子节点；无需额外参数。"],
    scenario: "已有条件可以判断某个状态，需要在相反状态下进入流程。",
    example: "顺序 → 取反（目标可见？）→ 搜索目标。目标不可见时取反成功，进入搜索；可见时不执行搜索。",
  },
  succeed: {
    description: "等待子节点结束，再将其终态统一转换为成功，允许外层流程忽略该步骤的失败。",
    results: ["子节点成功或失败都转换为成功；子节点 Running 时仍保持 Running。"],
    configuration: ["必须连接一个子节点；不会跳过子节点执行，也不会立即结束正在运行的子节点。"],
    scenario: "允许失败的附加步骤，例如非关键提示、效果或记录。",
    example: "顺序 → 强制成功（播放提示音）→ 提交任务。提示音播放失败也会继续提交；尚在播放时仍等待播放结束。",
  },
  fail: {
    description: "等待子节点结束，再将其终态统一转换为失败，常用于执行附加操作后让外层选择节点继续后续分支。",
    results: ["子节点成功或失败都转换为失败；子节点 Running 时仍保持 Running。"],
    configuration: ["必须连接一个子节点；不会省略子节点执行，也不会将 Running 提前转换为失败。"],
    scenario: "先记录状态、发送提示，再继续尝试其他处理方案。",
    example: "选择 → 强制失败（记录首次尝试）→ 执行备用方案。记录结束后第一个分支总是失败，因此继续备用方案。",
  },
  subtree: {
    description: "引用当前工程的另一棵行为树，复用完整流程。每个调用位置拥有独立的运行状态，可分别保留执行进度。",
    results: ["被引用树成功、失败或 Running 时，子树节点返回对应结果。"],
    configuration: ["在属性面板选择目标行为树，引用使用树 ID；展示名称可以重复，应核对 ID。", "子树节点不能连接本地子节点；禁止直接或间接递归引用。运行状态独立不代表拥有独立黑板。"],
    scenario: "将返回出生点、通用交互、战斗准备等流程抽成可复用的行为树。",
    example: "巡逻树和战斗树都引用“返回出生点”子树。两处调用各自记录进行到哪一步；修改被引用树的流程后，重新生成即可应用到这些调用位置。",
  },
};
